# AutoLayoutSwitch

<div align="center">

![Platform](https://img.shields.io/badge/platform-Windows-blue)
![.NET](https://img.shields.io/badge/.NET-8.0-purple)
![License](https://img.shields.io/badge/license-MIT-green)
![Language](https://img.shields.io/badge/language-C%23-blue)

**Легковесное приложение для автоматического переключения раскладки клавиатуры между русским и английским языками на Windows**

[Возможности](#-возможности) • [Установка](#-установка) • [Настройка](#️-настройка) • [Использование](#️-использование) • [Сборка](#-сборка-из-исходников)

</div>

---

Приложение работает в фоновом режиме, отслеживает ввод текста и автоматически переключает раскладку, если обнаруживает, что вы печатаете на неправильной раскладке. Также оно автоматически исправляет уже набранный текст.

---

## 🚀 Возможности

- **Автоматическое переключение раскладки**: Определяет, когда вы печатаете на неправильной раскладке, и автоматически переключает её.
- **Автоматическое исправление**: Автоматически исправляет последнее набранное слово (например, `ghbdtn` → `привет`).
- **Звуковое уведомление**: Воспроизводит звук при переключении раскладки (настраивается).
- **Ручное исправление**: Нажмите `Shift + F12` (по умолчанию, настраивается) для ручного исправления последнего слова.
- **Графический интерфейс настроек**: Удобное окно для настройки всех параметров приложения.
- **Настраиваемые исключения**: Добавьте слова, которые не должны вызывать переключение раскладки.
- **Автозапуск**: Приложение может автоматически запускаться вместе с Windows.
- **Иконка в системном трее**: Переключение и выход из приложения через меню в трее.
- **Единый исполняемый файл**: Не требует установки, просто запустите `.exe` файл.

---

## 📥 Установка

1. Скачайте последнюю версию `AutoLayoutSwitch.exe` из раздела [Releases](../../releases).
2. Поместите файл в любую удобную папку (например, `C:\Tools\AutoLayoutSwitch\`).
3. Запустите `AutoLayoutSwitch.exe`.
4. Приложение появится в системном трее (область уведомлений).

---

## ⚙️ Настройка

### Открытие окна настроек

1. Найдите иконку приложения в системном трее (две белые стрелки).
2. Нажмите **правой кнопкой мыши** на иконку.
3. Выберите **"Настройки"**.

### Доступные настройки

#### 🔊 Звук при переключении
- **Описание**: Включает или выключает звуковое уведомление при автоматическом переключении раскладки.
- **По умолчанию**: Включено.
- **Как настроить**: Установите или снимите галочку **"Звук при переключении"**.

#### 🚀 Запускать вместе с Windows
- **Описание**: Приложение будет автоматически запускаться при входе в систему.
- **По умолчанию**: Включено.
- **Как настроить**: Установите или снимите галочку **"Запускать вместе с Windows"**.

#### ⌨️ Горячая клавиша исправления
- **Описание**: Комбинация клавиш для ручного переключения раскладки и исправления последнего слова.
- **По умолчанию**: `Shift + F12`.
- **Как настроить**:
  1. Нажмите кнопку **"Изменить..."**.
  2. Нажмите желаемую комбинацию клавиш (например, `Ctrl + Space` или `Alt + Q`).
  3. Новая комбинация отобразится на экране.
  4. Нажмите **"Сохранить"** для применения.

#### 📝 Исключения
- **Описание**: Список английских слов, которые не должны вызывать переключение раскладки (например, технические термины, аббревиатуры).
- **По умолчанию**: `php`, `css`, `url`, `jpg`, `png`, `hmm`.
- **Как настроить**:
  1. В текстовом поле **"Исключения"** введите слова, по одному на каждой строке.
  2. Пример:
     ```
     php
     css
     html
     json
     api
     ```
  3. Нажмите **"Сохранить"**.

---

## 🎯 Как это работает

Приложение использует несколько эвристик для определения неправильной раскладки:

1. **Проверка гласных**: Слова из 3+ символов проверяются на наличие соответствующих гласных букв.
   - Если в английской раскладке набрано слово без английских гласных (a, e, i, o, u, y), но есть русские гласные на тех же клавишах — переключается на русский.
   - Аналогично для русской раскладки.

2. **Подозрительные символы**: Символы `[`, `]`, `;`, `'` внутри английского слова обычно соответствуют русским буквам — переключается на русский.

3. **Диапазоны символов**: Прямое определение латиницы или кириллицы в неправильной раскладке.

4. **Исключения**: Слова из списка исключений игнорируются (например, `php`, `css`).

5. **Аббревиатуры**: Слова, набранные полностью заглавными буквами (например, `PHP`), игнорируются проверкой гласных.

---

## 🛠️ Использование

### Автоматический режим
Просто печатайте как обычно. Если приложение обнаружит, что вы печатаете на неправильной раскладке, оно:
1. Переключит раскладку.
2. Удалит неправильно набранное слово.
3. Автоматически перепечатает его в правильной раскладке.
4. Воспроизведет звук (если включено).

### Ручное исправление
Если автоматическое определение не сработало:
1. Нажмите `Shift + F12` (или вашу настроенную комбинацию).
2. Приложение переключит раскладку и исправит последнее слово.

### Управление через трей
- **Включить/Выключить автопереключение**: Нажмите правой кнопкой на иконку → выберите **"Включить авто-переключение"** (галочка показывает текущее состояние).
- **Настройки**: Нажмите правой кнопкой на иконку → **"Настройки"**.
- **Выход**: Нажмите правой кнопкой на иконку → **"Выход"**.

---

## 📂 Файлы конфигурации

Все настройки хранятся в файле:
```
%LOCALAPPDATA%\AutoLayoutSwitch\settings.json
```

Пример содержимого:
```json
{
  "PlaySound": true,
  "AutoStart": true,
  "HotKeyVk": 123,
  "HotKeyModifiers": 4,
  "Exceptions": [
    "php",
    "css",
    "url",
    "jpg"
  ]
}
```

### Параметры файла настроек

| Параметр | Тип | Описание | Значение по умолчанию |
|----------|-----|----------|----------------------|
| `PlaySound` | `bool` | Воспроизводить звук при переключении | `true` |
| `AutoStart` | `bool` | Запускать с Windows | `true` |
| `HotKeyVk` | `int` | Код виртуальной клавиши (VK) для горячей клавиши | `123` (F12) |
| `HotKeyModifiers` | `uint` | Модификаторы: `1`=Alt, `2`=Ctrl, `4`=Shift, `8`=Win | `4` (Shift) |
| `Exceptions` | `string[]` | Список слов-исключений | `["php", "css", "url", ...]` |

**Примечание**: Рекомендуется использовать графический интерфейс настроек вместо ручного редактирования файла.

---

## 📋 Логи

Логи приложения сохраняются в:
```
%LOCALAPPDATA%\AutoLayoutSwitch\log.txt
```

Файл лога автоматически ротируется при достижении размера 1 МБ.

---

## 🔧 Сборка из исходников

### Требования
- .NET 8.0 SDK
- Windows 10/11

### Команды
```bash
cd src
dotnet publish -c Release -r win-x64
```

Готовый файл будет находиться в:
```
src\bin\Release\net8.0-windows\win-x64\publish\AutoLayoutSwitch.exe
```

---

## ❓ Часто задаваемые вопросы

### Приложение не переключает раскладку
1. Убедитесь, что приложение запущено (иконка в трее).
2. Проверьте, что автопереключение включено (правый клик → галочка на "Включить авто-переключение").
3. Проверьте логи в `%LOCALAPPDATA%\AutoLayoutSwitch\log.txt`.

### Некоторые слова переключаются неправильно
Добавьте эти слова в список исключений через окно настроек.

### Как изменить горячую клавишу?
1. Откройте настройки (правый клик на иконку → "Настройки").
2. Нажмите "Изменить..." рядом с "Горячая клавиша исправления".
3. Нажмите желаемую комбинацию клавиш.
4. Нажмите "Сохранить".

### Приложение не запускается автоматически с Windows
1. Откройте настройки.
2. Убедитесь, что галочка "Запускать вместе с Windows" установлена.
3. Нажмите "Сохранить".

---

## 📜 Лицензия

MIT License

---

## 🙏 Благодарности

Проект вдохновлен [KeyboardSwitch](https://github.com/TolikPylypchuk/KeyboardSwitch).

---

## 📧 Обратная связь

Если у вас есть предложения или вы нашли ошибку, пожалуйста, создайте [Issue](../../issues) в репозитории.
